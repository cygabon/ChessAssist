# Configuration ChessAssist
# Copiez ce fichier vers .env et modifiez les valeurs

# Nom d'utilisateur chess.com (optionnel)
# CHESS_COM_USERNAME=votre_nom_utilisateur

# Chemin vers l'exécutable Stockfish (optionnel, auto-détection)
# STOCKFISH_PATH=/usr/local/bin/stockfish

# Profondeur d'analyse Stockfish (par défaut: 15)
ANALYSIS_DEPTH=15

# Temps d'analyse par position en secondes (par défaut: 2.0)
ANALYSIS_TIME=2.0

# Délai entre les requêtes API chess.com en secondes (par défaut: 1.0)
API_RATE_LIMIT=1.0